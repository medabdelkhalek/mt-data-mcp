# Native accelerator helper for lean installs on Python 3.14.
#
# The full [all] stack includes this package. Use this helper when the lean
# package is installed and only the HNSW pattern-search accelerator is wanted.
# PyPI does not publish a Windows/cp314 wheel, so pip builds it from source.
#
# Windows prerequisites:
# - hnswlib: Visual Studio Build Tools 2022 with the Desktop development with C++ workload
#
# This source build is validated on Windows with the toolchain above.

hnswlib==0.8.0
